<% if obj.errors.any? %>
<div class="alert alert-danger alert-dismissable fade show" role="alert">
  <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
  </button>
  <h4 class="alert-heading"><%= pluralize(obj.errors.count, "error") %> prohibited this article from being saved:</h4>
  <ul>
        <% obj.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
  <hr>
  <p class="mb-0">Please fix erros before Creating Article</p>
</div>
  <% end %>
